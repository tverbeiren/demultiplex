apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workflow-workspace-pvc
  namespace: argo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  # Optional: specify storage class
  # storageClassName: fast-ssd